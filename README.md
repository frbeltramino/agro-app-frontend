# AdvanceStock

1. Clonar el repositorio
2. clonar el archivo .env.template y renombrarlo a .env
3. Ejecutar el comando `npm install` para instalar dependecias.
4. verificar el puerto donde está corriendo el backend.